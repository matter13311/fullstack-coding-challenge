module.exports = {
  purge: ["./src/**/*.{js,jsx,ts,tsx}"], // Specify the paths to your template files
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {}, // Extend Tailwind's default theme if needed
  },
  variants: {
    extend: {}, // Extend variants if needed
  },
  plugins: [],
};